{
  "name": "check",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run sass && eleventy",
    "sass": "node-sass -o dist/css --output-style compact sass",
    "serve": "npm-run-all sass --parallel watch:*",
    "watch:eleventy": "eleventy --serve",
    "watch:sass": "npm run sass -- --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^1.0.2"
  },
  "dependencies": {
    "liquid": "^5.1.1",
    "node-sass": "^8.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.57.1"
  }
}
